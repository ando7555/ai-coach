spring:
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: ai-coach   # change this

  graphql:
    schema:
      locations: classpath:graphql/
    graphiql:
      enabled: true

server:
  port: 8080
